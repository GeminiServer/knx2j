plugins {
  id 'java'
}

sourceCompatibility = '1.8'
targetCompatibility = '1.8'

version = '1.0.0'

jar {
  manifest {
    attributes('Main-Class': 'knx2j')
  }
}

task release(type: Copy, dependsOn: [jar]) {
  from jar
  from sourceSets.main.resources
  into 'release'
}
